/**
 * @deprecated Import from `@/lib/search/photo` instead.
 * Kept as a thin re-export for existing call sites.
 */
export {
  storeSearchPhoto,
  readSearchPhoto,
  clearSearchPhoto,
  readImageFile,
  type StoredSearchPhoto,
} from "@/lib/search/photo";
